using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace CarTag {

    public class GameBalanceManager : MonoBehaviour {
        //distance to travel
        //runner boxes
        //Chaser boxes
        // runner rocket
        // chaser rocket
        // runner Wait Time 
        //Chaser Wait time Round Start
        // chaser Wait time Role Swap
        // checkpoint trigger radius ???

        // Start is called before the first frame update
        void Start() {

        }

        // Update is called once per frame
        void Update() {

        }
    }
}
