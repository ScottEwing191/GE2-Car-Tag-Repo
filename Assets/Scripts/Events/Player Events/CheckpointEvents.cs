using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace CarTag.Events
{
    public class CheckpointEvents 
    {
        /// <summary>
        /// Invoked when player is getting too far a way from the checkpoint
        /// </summary>
        /*public Action distanceToCheckpointIncreases = delegate {  };
        public void OnDistanceToCheckpointIncreases() {
            distanceToCheckpointIncreases?.Invoke();
        }*/
    }
}
